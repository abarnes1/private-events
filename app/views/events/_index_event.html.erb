<% unless event.private? %>
  <div class="event-card">
    <div class="event-card-header">
      <%= event.name %> on <%= event.date %> | <%= link_to 'Details', event_path(event) %>
    </div>
  </div>
<% end %>